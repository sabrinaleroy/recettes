{% extends 'base.html.twig' %}

{% block title %}Instruction{% endblock %}

{% block body %}
    <h1>Instruction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instruction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ instruction.name }}</td>
            </tr>
            <tr>
                <th>Sort</th>
                <td>{{ instruction.sort }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instruction_index') }}">back to list</a>

    <a href="{{ path('instruction_edit', {'id': instruction.id}) }}">edit</a>

    {{ include('instruction/_delete_form.html.twig') }}
{% endblock %}
