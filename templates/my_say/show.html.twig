{% extends 'base.html.twig' %}

{% block title %}MySay{% endblock %}

{% block body %}
    <h1>MySay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ my_say.id }}</td>
            </tr>
            <tr>
                <th>Entity</th>
                <td>{{ my_say.entity }}</td>
            </tr>
            <tr>
                <th>EntityId</th>
                <td>{{ my_say.entityId }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ my_say.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('my_say_index') }}">back to list</a>

    <a href="{{ path('my_say_edit', {'id': my_say.id}) }}">edit</a>

    {{ include('my_say/_delete_form.html.twig') }}
{% endblock %}
