<form method="post" action="{{ path('com_note_delete', {'id': com_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ com_note.id) }}">
    <button class="btn">Delete</button>
</form>
