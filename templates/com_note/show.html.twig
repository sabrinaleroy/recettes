{% extends 'base.html.twig' %}

{% block title %}ComNote{% endblock %}

{% block body %}
    <h1>ComNote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ com_note.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ com_note.date ? com_note.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ com_note.note }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ com_note.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('com_note_index') }}">back to list</a>

    <a href="{{ path('com_note_edit', {'id': com_note.id}) }}">edit</a>

    {{ include('com_note/_delete_form.html.twig') }}
{% endblock %}
