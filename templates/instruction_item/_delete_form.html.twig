<form method="post" action="{{ path('instruction_item_delete', {'id': instruction_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instruction_item.id) }}">
    <button class="btn">Delete</button>
</form>
