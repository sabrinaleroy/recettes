{% extends 'base.html.twig' %}

{% block title %}InstructionItem{% endblock %}

{% block body %}
    <h1>InstructionItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instruction_item.id }}</td>
            </tr>
            <tr>
                <th>Quotient</th>
                <td>{{ instruction_item.quotient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instruction_item_index') }}">back to list</a>

    <a href="{{ path('instruction_item_edit', {'id': instruction_item.id}) }}">edit</a>

    {{ include('instruction_item/_delete_form.html.twig') }}
{% endblock %}
