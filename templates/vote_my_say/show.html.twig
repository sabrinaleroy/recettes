{% extends 'base.html.twig' %}

{% block title %}VoteMySay{% endblock %}

{% block body %}
    <h1>VoteMySay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vote_my_say.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ vote_my_say.value ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vote_my_say_index') }}">back to list</a>

    <a href="{{ path('vote_my_say_edit', {'id': vote_my_say.id}) }}">edit</a>

    {{ include('vote_my_say/_delete_form.html.twig') }}
{% endblock %}
