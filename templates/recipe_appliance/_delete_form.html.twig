<form method="post" action="{{ path('recipe_appliance_delete', {'id': recipe_appliance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_appliance.id) }}">
    <button class="btn">Delete</button>
</form>
