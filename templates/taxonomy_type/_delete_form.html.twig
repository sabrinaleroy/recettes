<form method="post" action="{{ path('taxonomy_type_delete', {'id': taxonomy_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxonomy_type.id) }}">
    <button class="btn">Delete</button>
</form>
