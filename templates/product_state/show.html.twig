{% extends 'base.html.twig' %}

{% block title %}ProductState{% endblock %}

{% block body %}
    <h1>ProductState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_state.id }}</td>
            </tr>
            <tr>
                <th>LabelF</th>
                <td>{{ product_state.labelF }}</td>
            </tr>
            <tr>
                <th>LabelM</th>
                <td>{{ product_state.labelM }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_state_index') }}">back to list</a>

    <a href="{{ path('product_state_edit', {'id': product_state.id}) }}">edit</a>

    {{ include('product_state/_delete_form.html.twig') }}
{% endblock %}
