{% extends 'base.html.twig' %}

{% block title %}Appliance{% endblock %}

{% block body %}
    <h1>Appliance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appliance.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ appliance.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ appliance.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('appliance_index') }}">back to list</a>

    <a href="{{ path('appliance_edit', {'id': appliance.id}) }}">edit</a>

    {{ include('appliance/_delete_form.html.twig') }}
{% endblock %}
